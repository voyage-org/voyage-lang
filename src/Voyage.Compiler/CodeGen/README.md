# CodeGen/

Not yet implemented, and deliberately not yet designed.

IR → CIL emission. ADR-0010 explicitly deferred this phase's architecture
(which CIL-emission approach/library, generic monomorphization strategy —
already flagged as its own future ADR in `type-system.md`'s open items) as
premature relative to what's actually been decided so far. Needs its own
ADR before implementation starts here.
