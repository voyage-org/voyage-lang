# Voyage.Integration.Tests

Not yet started. Intended for end-to-end tests once the full pipeline
(`Lexing/` → `Parsing/` → `Semantics/` → `Lowering/` → `CodeGen/`) can
actually compile and run a `.voy` program, rather than testing phases in
isolation the way `Voyage.Compiler.Tests` currently does.
